# data

Reference lookup tables MediaHub reads (swim strokes, courses, governing bodies)
plus example brand kits and voices. When the app runs it also saves its working
files under here — those saved files are not part of the code and are ignored by
git.
