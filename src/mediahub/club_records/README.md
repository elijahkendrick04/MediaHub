# club_records

The club records book (W.3). Import the records sheet once; when a swim beats
a stored mark, a "NEW CLUB RECORD" card appears ranked above everything else.
The table itself only changes when a human approves that card — detection
alone never rewrites history.
