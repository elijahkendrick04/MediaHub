# safeguarding

The photo/name permission book (W.2). Each athlete can be: photo + name OK,
name only, initials only, or never featured. Once a club starts using it,
anyone with no permission on file is automatically treated as "never
featured" — cards are blocked at build time AND at the publish gate, and the
welfare officer can export the whole register as a spreadsheet.
