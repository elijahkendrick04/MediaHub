# content_engine

The single writer that makes all the captions. It first *plans* the set of posts
(which angle, which platform), then *writes* each caption. Every content type goes
through here, so there isn't a separate writer for each.

Plain-English words: see ../../../GLOSSARY.md
