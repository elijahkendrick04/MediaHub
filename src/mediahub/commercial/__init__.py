"""Phase C commercial instrumentation (PC.4 + PC.6) — operator-facing.

Revealed willingness-to-pay quotes (`wtp`), the warm-first sales pipeline
(`pipeline`), and the Swim England approved-systems API application state
(`ngb`). Surfaced on the operator-only `/operator/commercial` page.
"""
