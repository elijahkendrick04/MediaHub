# standards

Loads the season's qualifying-time tables (W.4) — "swim faster than this and
you've made Counties". Tables live in `data/standards/<season>/` with the
source PDF and date written on every one, and each club ticks the
competitions it cares about in Organisation settings.
