# Dependency Map

Per-package internal dependencies. `A → B` means files in package `A` import from package `B`.

_Auto-generated by walking `src/mediahub/` and grepping `from mediahub.<pkg>`._

| Package | Depends on |
|---|---|
| `ai_core` | `brand`, `media_ai`, `web` |
| `assistant` | `ai_core`, `club_platform`, `content_engine`, `creative_brief`, `graphic_renderer`, `quality`, `visual` |
| `athletes` | `workflow` |
| `autonomy` | `ai_core`, `notify`, `scheduler`, `web`, `workflow` |
| `brand` | `graphic_renderer`, `media_ai`, `theming`, `web`, `web_research` |
| `club_platform` | `content_engine`, `sport_profiles`, `workflow` |
| `club_qa` | `ai_core`, `athletes` |
| `commercial` | `web` |
| `compliance` | `athletes`, `media_library`, `memory`, `notify`, `privacy`, `safeguarding`, `web` |
| `content_engine` | `ai_core`, `brand`, `club_platform`, `context_engine`, `sport_profiles`, `web`, `workflow` |
| `content_pack` | `recognition`, `safeguarding`, `web`, `workflow` |
| `content_pack_visual` | `brand`, `compliance`, `creative_brief`, `graphic_renderer`, `media_library`, `media_requirements`, `quality`, `theming`, `web` |
| `context_engine` | `web_research` |
| `creative_brief` | `ai_core`, `graphic_renderer`, `inspiration`, `media_ai` |
| `free_text_chat` | `ai_core`, `context_engine` |
| `graphic_renderer` | `media_ai`, `media_library`, `quality`, `theming`, `visual` |
| `inspiration` | `media_ai` |
| `log_sentinel` | `ai_core`, `notify` |
| `media_ai` | `ai_core`, `graphic_renderer`, `observability`, `web` |
| `media_library` | `graphic_renderer`, `media_ai` |
| `media_requirements` | `media_library` |
| `memory` | `ai_core` |
| `motion` | `visual` |
| `pb_discovery` | `context_engine`, `interpreter`, `web_research` |
| `pipeline` | `compliance`, `interpreter`, `media_ai`, `pb_discovery`, `web` |
| `privacy` | `athletes`, `brand`, `context_engine`, `creative_brief`, `graphic_renderer`, `media_library`, `memory`, `pb_discovery`, `safeguarding`, `web`, `web_research`, `workflow` |
| `quality` | `graphic_renderer`, `theming` |
| `recognition_swim` | `athletes`, `club_records`, `recognition` |
| `results_fetch` | `ai_core`, `athletes`, `interpreter`, `media_ai`, `notify`, `scheduler`, `web_research` |
| `safeguarding` | `athletes`, `workflow` |
| `scheduler` | `workflow` |
| `season_wrap` | `notify`, `scheduler` |
| `turn_into` | `brand`, `club_platform`, `creative_brief`, `graphic_renderer`, `media_ai`, `voice`, `web` |
| `video` | `graphic_renderer`, `media_ai`, `media_library`, `visual` |
| `visual` | `creative_brief`, `graphic_renderer`, `media_ai`, `media_library`, `theming` |
| `voice` | `web` |
| `web` | `ai_core`, `assistant`, `athletes`, `autonomy`, `backup`, `brand`, `club_platform`, `club_qa`, `club_records`, `commercial`, `compliance`, `content_engine`, `content_pack`, `content_pack_visual`, `context_engine`, `creative_brief`, `free_text_chat`, `graphic_renderer`, `interpreter`, `log_sentinel`, `media_ai`, `media_library`, `memory`, `mockups`, `motion`, `notify`, `observability`, `pipeline`, `privacy`, `recognition`, `results_fetch`, `safeguarding`, `scheduler`, `season_wrap`, `sport_profiles`, `standards`, `theming`, `turn_into`, `venue_search`, `video`, `visual`, `voice`, `web_research`, `workflow` |
| `web_research` | `ai_core`, `context_engine`, `web` |
| `workflow` | `brand`, `memory`, `web` |

## External Python dependencies

```
Flask>=3.1.3
gunicorn>=26.0.0
requests>=2.34.2
beautifulsoup4>=4.15.0
lxml>=6.1.1
pypdf>=6.13.2
# Floor must stay <= the exact pdfminer.six that pdfplumber pins (0.11.9 pins
# ==20251230); dependabot's >=20260107 bump made the pair unresolvable and
# broke pip-audit (ResolutionImpossible).
pdfminer.six>=20251230
pdfplumber>=0.11.9
# Spreadsheet ingestion (interpreter/ingest.py _extract_xlsx). Leagues and meets
# publish results as .xlsx; openpyxl reads them deterministically (read-only).
openpyxl>=3.1.5

# V8: media generation engine. Playwright pinned to the version whose
# bundled Chromium revision matches what's reliably installable on
# Render at build time. See pyproject.toml comment for the full reason.
playwright>=1.60.0,<1.61
pillow>=12.2.0
# 1.3 photo editor: decode iPhone HEIC/HEIF uploads to web-safe JPEG on ingest
# (media_library.heic). BSD-3 wrapper over bundled libheif; manylinux wheels
# need no system libs. Decode-only at upload time; the code honest-errors on a
# HEIC upload if it's ever absent.
pillow-heif>=0.16.0
rembg>=2.0.76
onnxruntime>=1.26.0
anthropic>=0.109.1
replicate>=1.0.7

# P0.1 free reel fallback (MEDIAHUB_REEL_ENGINE=ffmpeg): ships a static FFmpeg
# binary so the fallback needs no system ffmpeg and no Remotion license. The
# wheel is BSD-2; the bundled FFmpeg is invoked strictly as a subprocess.
imageio-ffmpeg>=0.6.0

# Capability 2 (semantic memory): sqlite-vec is a pure-C, dependency-free
# SQLite extension (MIT/Apache-2.0 dual) that stores vectors in a vec0 virtual
# table inside MediaHub's own SQLite file and does exact brute-force KNN. Kept
# behind mediahub.memory.store so the backend stays swappable. Pinned to a
# known-good 0.1.x (the project is pre-1.0).
sqlite-vec>=0.1.9,<0.2

# Voiceover (optional, operator-gated via MEDIAHUB_VOICEOVER=1): deterministic
# text-to-speech of the already-approved caption. When absent the voiceover
# route honest-errors (503) rather than degrading to a fake voice.
#
# DEFAULT backend (roadmap 1.7): local Piper (piper-tts, GPL-3.0-or-later) —
# zero-cost, fully offline; the caption text never leaves the box. It is not
# pinned here on purpose: the package + a licence-clean CC BY 4.0 voice model
# are installed in the DEPLOYED image (see Dockerfile + scripts/fetch_piper_voice.py),
# the same shape as the W.10 OCR engine. onnxruntime/ffmpeg (its only heavy deps)
# already ship above. For a non-Docker dev box that wants offline narration:
#   pip install piper-tts            # then drop a '.onnx' in $DATA_DIR/piper_voices
# Absent → an honest VoiceoverError, never a fabricated voice.
#
# OPTIONAL online alternative: edge-tts (Microsoft endpoint — caption text
# leaves the box), selected with MEDIAHUB_TTS_PROVIDER=edge. No longer the
# default (1.7) and no longer a hard dependency; install only if you want it:
#   pip install edge-tts

# Local, zero-cost ASR backend (roadmap 1.4), selected with
# MEDIAHUB_ASR_PROVIDER=faster-whisper. MIT; a CTranslate2 reimplementation of
# OpenAI Whisper that transcribes fully offline on CPU with word-level
# timestamps — the primitive for the copilot's uploaded-audio voice input and
# the video suite's word-level captions (1.6). Left OPTIONAL on purpose: server
# ASR is off by default (the browser's on-device speech is the live voice path)
# and the CTranslate2 weights are heavy. Install it only when you want server
# transcription:
#   pip install faster-whisper       # or: pip install pywhispercpp  (whisper.cpp)
# Absent → an honest ASRUnavailable, never a fabricated transcript.

# Phase 1.6 Stage B — Adaptive Theming Engine colour-science package.
# materialyoucolor is the maintained Python port of Google's
# material-color-utilities; it ships HCT, the MD3 TonalSpot scheme
# builder, the Material You quantizer (Wu + WSMeans → Score) used by
# Android 12 Monet, and the 50+ dynamic-color role tokens.
# coloraide is the pure-Python W3C CSS Color 4/5 reference impl
# (OKLCH conversions, gamut mapping, CIEDE2000).
# numpy is used by the theming CVD simulator (3×3 Machado matrices),
# the saliency cropper (energy maps), and variant_metrics (perceptual hashing).
# See docs/THEMING.md for the architecture.
materialyoucolor>=3.0.2
coloraide>=8.8.1
numpy>=2.4.6

# Sport-profile config loader (mediahub.sport_profiles) reads the human-authored
# data/sport_profiles/*.yaml files. PyYAML is MIT-licensed and free; the loader is
# inert scaffolding (not yet wired into the runtime pipeline).
PyYAML>=6.0.3

# G1.10 club custom-font upload pipeline (mediahub.typography.font_intake):
# validates, subsets to MediaHub's Latin set, and re-packs an uploaded brand
# typeface as a first-party self-hosted WOFF2 (never the Google Fonts CDN). The
# [woff] extra pulls in brotli (+ zopfli) for WOFF2 (de)compression. fontTools is
# MIT-licensed. The module honest-errors (FontToolingUnavailable) if it's absent;
# its pure-Python security validation still rejects unsafe uploads with no deps.
fonttools[woff]>=4.56.0

# Scheduler (mediahub.scheduler / mediahub.workflow.schedule) uses croniter to
# compute cron / daily / weekly / monthly fire-times. croniter is MIT-licensed,
# maintained under the Pallets-Eco (Flask) org; deps python-dateutil + pytz.
croniter>=6.2.2

# Phase C commercial layer (web/auth.py + web/billing.py). bcrypt (Apache-2.0)
# hashes account passwords — used directly because passlib 1.7.x cannot load a
# bcrypt 4.1+/5.x backend. stripe (MIT) is the official SDK for Checkout, the
# Customer Portal, and webhook signature verification. Both are optional at
# runtime: with no STRIPE_* env configured, billing honest-errors (503) and the
# rest of the app is unaffected.
bcrypt>=5.0.0
# argon2-cffi (MIT): argon2id password hashing (ASVS L2 V2.4); bcrypt stays
# for verifying legacy hashes, upgraded on next login.
argon2-cffi>=25.1.0
stripe>=15.2.0

# W.10 OPTIONAL OCR fallback (interpreter/ocr.py) — deliberately NOT installed
# here. Scanned/photographed result sheets need a real OCR engine; without one
# the interpreter keeps the honest "image-needs-ocr" review flag instead of
# fabricating text. The deployed Docker image installs the engine (apt
# tesseract-ocr + pip pytesseract) and the scanned-PDF page rasteriser
# (pypdfium2) — see Dockerfile. Elsewhere, opt in with the `ocr` extra
# (pyproject: pip install -e ".[ocr]", needs the tesseract binary on PATH) or
# the pure-pip alternative: pip install rapidocr-onnxruntime pypdfium2

```
