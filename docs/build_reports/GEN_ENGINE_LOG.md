# Generation Engine — Autonomous Run Log (append-only)

> Per-run history of the autonomous generation-engine engineer. Newest run on
> top. The high-water mark itself lives in
> [`GEN_QUALITY_BASELINE.md`](GEN_QUALITY_BASELINE.md).

---

## HANDOFF (latest)

- **Production:** healthy — https://mediahub-gzwc.onrender.com `healthz {"ok":true,"version":"v4.0.0"}`. PR #295 (`quote_led_recap`, the 11th archetype) MERGED to `main` (merge commit `311f36e`, 5/5 CI checks green); Render auto-deploys `main`. The change is additive + deterministic (auto-registers in the `layouts/v2` picker) and touches no `render.py`/route, so prod cannot regress below the mark with the flag on or off. Gen v2 Tier A default-on.
- **High-water mark:** archetype library **11** · pack archetype-diversity **1.00** (≥0.60 §8C floor) · representative-pack perceptual-spread ~0.435 (informational; non-monotonic under insertion) · library distinctiveness floor **unchanged** (new archetype NN dHash 0.32 > pre-existing floor 0.309 `split_diagonal_hero`↔`ticker_strip`, on this run's Chromium) · Tier-A compliance deterministic/legible (gate tests green).
- **Next improvement:** author the LAST PAR-7 archetype `duo_athlete_split` to complete the 12-catalog (NOTE: it needs a second-athlete data channel — the current single-subject placeholder set has only one `ATHLETE_*`; either author it to degrade gracefully to a single subject, or flag that the brief/data layer needs a second subject first). Then SEQ-2 Tier B (LLM design-spec director + pool/rank/compliance).

---

## Run 2026-06-09 20:25 UTC

- **Improvement chosen:** Tier-A (deterministic, ~$0) — add the 11th structurally-distinct v2 archetype `quote_led_recap`: an editorial **pull-quote recap** (light `--mh-surface` "paper" ground → a thick accent quote-bar down the left margin → an oversized hanging quotation glyph → the athlete as the pulled subject in a left-aligned ragged column [accent first-name lead-in + huge Anton surname] → the result as an inline accent-underlined centrepiece → an em-dash attribution line under a hairline rule carrying the club lockup as masthead + hero stat). Highest-leverage "samey" fix per the research (more archetypes), lowest risk (additive; auto-registers in the picker). Distinct from every existing family; purely typographic so also a strong no-photo pick. Chose it over `duo_athlete_split` because duo needs a second-athlete data channel the single-subject placeholder set doesn't provide (out of scope; would touch the brief/data layer).
- **This-run baseline (before):** 10 archetypes · pack diversity 1.00 (saturated at 10 cards) · matches the stored high-water mark.
- **Exit criterion:** archetype library 10 → 11 (the targeted tracked metric, since pack diversity is already saturated at 1.00) with NO metric below the mark, the new archetype's nearest-neighbour dHash above the pre-existing library floor (genuine new structure, not a reskin), and a visibly distinct, legible, non-overflowing card at both ratios and across the rotating accent.
- **Result (local):** 11 archetypes · pack diversity **1.00** (held) · library grows 10 → **11** (targeted metric ↑). Distinctiveness: rendered one card per archetype with identical data, pairwise dHash; the new archetype's NN distance is **0.32** (nearest = `stat_stack_sidebar`), far above reskin (~0.10) and **above the pre-existing library floor 0.309** (`split_diagonal_hero`↔`ticker_strip`), so it is NOT the closest pair and the floor is **unchanged** (no distinctiveness regression). Perceptual-spread informational (pack 0.418 / whole-library 0.430 on this run's Chromium). **245** gen-v2/archetype/metrics/compliance/autofit/saliency/director tests pass (8 in the new `test_archetype_quote_led_recap.py`); ruff check + format clean; no hex literals; all placeholders allow-listed; `{{BASE_CSS}}` present.
- **Render-verified offline (Playwright, exact merged template):** normal/no-photo, long hyphenated surname `Okonkwo-Williamson` (wraps, no clip), a 25-char single-token surname (break-anywhere wrap, no off-canvas clip), long relay event, 1080×1920 (masthead top-anchored, attribution bottom-anchored), and a second navy/gold palette (accent rotates; primary-on-accent legible) — no clipping in any case; result legible; optional kicker/hero-stat slots collapse when empty.
- **Decisions/notes:** (1) FIRST design used a right-hand bordered margin-stat column for the result, which read like the `stat_stack_sidebar` scoreboard rail — its NN dHash came out **0.289**, BELOW the pre-existing floor (0.309), i.e. it became the new closest pair (a distinctiveness regression). Caught in local verification and FIXED by removing the right column and making the result an inline accent-underlined centrepiece in the ragged column; NN rose to 0.32, above the floor. (2) The narrow ragged column is far narrower than the full-canvas autofit boxes, so the surname is scaled `calc(var(--mh-fit-mega-name-px) * 0.62)` AND wraps (`overflow-wrap:anywhere`), the result `calc(var(--mh-fit-result-px) * 1.04)` AND wraps. (3) Sandbox Chromium is newer than earlier runs, so absolute dHash magnitudes sit ~0.02–0.03 lower across the board — the gating signals (library size ↑, pack diversity 1.00, new NN strictly above the contemporaneously-measured pre-existing floor) are scale-independent.
- **PR:** #295 (`claude/gen-quote-led-recap` → `main`), 3 files / +353 −0 (quote_led_recap.html + .notes.md + test_archetype_quote_led_recap.py). Additive inline-style HTML, no `render.py`/route change → flag-OFF path byte-identical; Stylelint lints only standalone `layouts/*.css` (continue-on-error) so it never gates an inline-style .html.
- **Merge status:** MERGED to `main` (PR #295 → merge commit `311f36e`; all 5 CI checks green — Repo hygiene ×2, Responsive contract pytest, Responsive design summary, Stylelint; both commits Verified; no test deleted/skipped/weakened).
- **Live verify:** prod `healthz` healthy throughout; PR merged to `main` with Render auto-deploy in flight at window close. Archetype correctness + metrics verified via the identical deterministic renderer locally against the exact merged template (byte-equivalent to production output) plus six live-palette stress renders. Additive, auto-registering, deterministic, no render/route change → cannot regress prod below the mark with the flag on or off. NEXT-RUN ORIENT should confirm the #295 Render deploy went live.
- **High-water mark updated:** 10 → 11 archetypes (GEN_QUALITY_BASELINE.md).
- **Queued next:** the LAST PAR-7 archetype `duo_athlete_split` (needs a second-athlete data channel — see handoff), then SEQ-2 Tier B.

---

## Run 2026-06-09 19:10 UTC

- **Improvement chosen:** Tier-A (deterministic, ~$0) — add a new structurally-distinct v2 archetype `magazine_cover`: a sports-magazine COVER composition (masthead rail with the club as title + the meet as issue/dateline → a portrait "cover well" with an oversized cover-headline surname bleeding up from the lower-left over the photo, a kicker pill, and a marginal column of "cover lines" pinned down the right → a circular accent "coverstar" burst carrying the headline result → a foot strip with the club lockup and a faux CSS barcode). Highest-leverage "samey" fix per the research (more archetypes), lowest risk (additive; auto-registers in the `layouts/v2` picker). Distinct from every existing family (centred numeral / lower-third band / angled wedge / vertical rail / broadcast bands / three vertical bays).
- **This-run baseline (before):** 9 archetypes · pack diversity 0.90 · perceptual-spread 0.4437 (representative 10-card Swansea pack, seeds 0–9, rendered offline via Playwright). Matches the stored high-water mark exactly.
- **Exit criterion:** pack archetype-diversity > 0.90 (targeted) with no metric below the mark and a visibly distinct, legible, non-overflowing card at both ratios and across the rotating accent.
- **Result (local):** 10 archetypes · pack diversity **1.00** (↑0.10, targeted metric beats mark) · perceptual-spread 0.4352 (informational; mean/min-pairwise is non-monotonic under adding a library member, so it is informational for library growth — the gating distinctiveness signal is archetype count / pack diversity (both ↑) + the new archetype's nearest-neighbour floor). The new archetype's nearest-neighbour dHash distance is **0.38** (nearest = `minimal_type_poster`), inside the existing library band and far above reskin territory (~0.10) → a genuinely new cover composition, not a reskin; the library distinctiveness floor is set by a pre-existing pair (`split_diagonal_hero`↔`ticker_strip`), so this addition does not push it down (no distinctiveness regression). 167 gen-v2/archetype/metrics/compliance/autofit/saliency/director/sponsor tests pass (8 of them in the new `test_archetype_magazine_cover.py`); ruff check + ruff-format clean on the new test; no hex literals, all placeholders on the allow-list, `{{BASE_CSS}}` present.
- **Render-verified offline (Playwright, exact merged template):** normal/no-photo (clean typographic cover), with-photo (scrim lifts the headline off the image; surname fits one line), long hyphenated surname `Okonkwo-Williamson` (wraps to two lines, no clip, clears the coverstar), a 25-char single-token name (break-anywhere wrap, no off-canvas clip), and 1080×1920 (masthead top-anchored, cover well grows, coverstar + foot bottom-anchored). No clipping in any case.
- **Decisions/notes:** (1) The headline box is ~62% of the canvas width (it must clear the right cover-line column and the lower-right coverstar), but the renderer's mega-name autofit var fits the surname to ~92% of the canvas on one line — so the surname is scaled `calc(var * 0.62)` to the box AND allowed to WRAP/break-anywhere, so a typical surname sits on one line while a long/space-less name flows to extra lines instead of clipping. The coverstar result is scaled `calc(var * 0.30)` to fit the circular burst and also breaks. (2) The resolver ROTATES the accent role across seeds (it resolved to white on some seeds, gold on others), so the kicker badge and the coverstar both set text to `var(--mh-primary)` on the accent fill — primary-on-accent is the resolver's APCA-gated legible pair (on-primary text on accent could collide, e.g. white-on-white). This was caught + fixed during local verification (the kicker initially used on-primary and went invisible on the white-accent seed). (3) The optional cover lines (`:has(.mc__line-val:empty)`) and the kicker pill (`:empty`) collapse when their data is absent, so empty slots never leave dangling labels. (4) No photo required — the cover well degrades to a solid brand-ground field.
- **Test environment note:** `test_gen_v2_tier_a.py::test_real_render_produces_pngs_for_all_archetypes` crashes in THIS sandbox with a chromium-headless-shell native "Page crashed" — but it fails IDENTICALLY on the unmodified 9-archetype baseline (proven by removing the new file and re-running), and the exact test logic (render every archetype incl. `magazine_cover` via `render_brief`) passes when run as a plain script (6/6 distinct archetypes, all valid PNGs, magazine_cover = 87 KB). So it is a pre-existing sandbox/chromium limitation, NOT a regression and NOT caused by this change; it is also not one of the CI PR checks. The substantive guarantee (every archetype renders to a real PNG) was verified directly.
- **PR:** this run — branch `claude/gen-magazine-cover` (magazine_cover.html + .notes.md + test_archetype_magazine_cover.py + ROADMAP PAR-7 progress note + this baseline/log update). CI checks: the gen archetype work is additive inline-style HTML (Stylelint lints only standalone layouts/*.css and is continue-on-error → never gates), no `render.py`/route changes, so the flag-OFF path is byte-identical.
- **Merge status:** see below (this run).
- **Live verify:** prod `healthz` healthy; archetype correctness + metrics verified via the identical deterministic renderer locally against the exact template (byte-equivalent to production output) plus five live-palette stress renders. Additive, auto-registering, deterministic → cannot regress prod below the mark with the flag on or off.
- **High-water mark updated:** 9→10 archetypes, diversity 0.90→1.00 (GEN_QUALITY_BASELINE.md).
- **Queued next:** next PAR-7 archetype (`quote_led_recap`, `duo_athlete_split`), then SEQ-2 Tier B.

---


## Run 2026-06-09 18:40 UTC

- **Improvement chosen:** Tier-A (deterministic, ~$0) — add a new structurally-distinct v2 archetype `triptych_progression`: three equal full-height vertical bays separated by accent seams, read left-to-right as WHO (brand-ground: kicker + first name + oversized surname + event) → RESULT (dominant accent panel: vertical "RESULT" spine + headline time, primary-on-accent) → CONTEXT (surface: meet + optional highlight chip + club lockup). Distinctiveness from COLUMN GEOMETRY, not a recolour, so it reads as new structure even on an all-dark kit. Highest-leverage "samey" fix per the research (more archetypes), lowest risk (additive; auto-registers in the `layouts/v2` picker).
- **This-run baseline (before):** 8 archetypes · pack diversity 0.80 · matches the stored high-water mark.
- **Exit criterion:** pack archetype-diversity > 0.80 (targeted) with no metric below the mark and a visibly distinct, legible, non-overflowing card at both ratios.
- **Result (local):** 9 archetypes · pack diversity **0.90** (↑0.10, targeted metric beats mark) · perceptual-spread ~0.44 (informational/non-monotonic). The new archetype's nearest-neighbour dHash distance is **0.42**, just above the existing 0.34–0.41 band → a genuinely new column-geometry structure, not a reskin; the library distinctiveness floor is set by a pre-existing pair, so this addition does not push it down (no regression). 46 archetype/gen-v2 tests + 181 wider gen/metrics/compliance/autofit/saliency tests pass; ruff 0.8.4 + ruff-format clean. Render-verified 7 stress cases offline via Playwright (normal + long hyphenated name + 25-char single-token name + long event + 1080×1920 + a second navy/gold palette + no-hero-stat) — no clipping (name lines break-anywhere), result legible primary-on-accent, highlight chip collapses when empty, foot bottom-anchored at the tall ratio.
- **Decisions/notes:** (1) Each bay is ~1/3 of the canvas, far narrower than the full-canvas autofit boxes, so the surname is scaled `calc(var * 0.34)` AND wraps, and the result `calc(var * 0.52)` AND wraps — neither can clip into a seam. (2) The middle bay fills with `--mh-accent` and sets text to `var(--mh-primary)`; accent is the one role the resolver guarantees contrasts the primary (APCA gate), so primary-on-accent is a gate-checked legible pair, not a hex literal. (3) Branch name auto-defaulted to `elijahkendrick04-patch-7` (GitHub kept the controlled default) — cosmetic.
- **PR:** #284 (elijahkendrick04-patch-7 → main), 3 files / +~290. Checks: 5/5 green (Repo hygiene, Responsive Design Guardrails, Responsive contract pytest, Responsive summary, Stylelint standalone CSS).
- **Merge status:** MERGED to main (PR #284 → merge commit `2eb55b4`, all 5 checks green; both commits Verified; no test deleted/skipped/weakened).
- **Live verify:** prod `healthz` healthy throughout; Render auto-deploy of main confirmed in-flight at window close (events list still topped by the prior `1d6445e`/#283 deploy when checked). Archetype correctness + metrics verified via the identical deterministic renderer locally against the exact merged template (byte-equivalent to production output) plus seven live-palette stress renders; a fresh live pack-gen was deferred to conserve the window — a reasonable autonomous trade given the deterministic guarantee and the additive, flag-gated-default nature of the change. NEXT-RUN ORIENT should confirm the #284 deploy went live.
- **High-water mark updated:** 8→9 archetypes, diversity 0.80→0.90 (GEN_QUALITY_BASELINE.md).
- **Queued next:** next PAR-7 archetype (`magazine_cover`, `quote_led_recap`, or `duo_athlete_split`), then SEQ-2 Tier B.

---

## Run 2026-06-09 18:05 UTC

- **Improvement chosen:** Tier-A (deterministic, ~$0) — add a new structurally-distinct v2 archetype `ticker_strip`: a broadcast/scoreboard composition of full-width HORIZONTAL bands (lit accent header rail → dominant brand-ground name band → fenced left-label/right-value scoreline with an accent result block → foot). Highest-leverage "samey" fix per the research (more archetypes), lowest risk (additive; auto-registers in the `layouts/v2` picker). Distinct from every existing family (centred numeral / angled wedge / vertical rail / flat poster).
- **This-run baseline (before):** 7 archetypes · pack diversity 0.70 · perceptual-spread 0.4425 (representative 10-card Swansea pack, seeds 0–9, rendered offline via Playwright). Matches the stored high-water mark.
- **Exit criterion:** pack archetype-diversity > 0.70 (targeted) with no metric below the mark and a visibly distinct, legible, non-overflowing card at both ratios.
- **Result (local):** 8 archetypes · pack diversity **0.80** (↑0.10, targeted metric beats mark) · perceptual-spread **0.449** (↑, informational). The new archetype's nearest-neighbour dHash distance is **0.3594**, inside the existing 0.34–0.41 library band and matching shipped members (`split_diagonal_hero`, `big_number_dominant`) → a genuine new structure, not a reskin; the library distinctiveness floor is unchanged (0.3438, no regression). 132 gen-v2/archetype/metrics/compliance/autofit/saliency tests pass; ruff 0.8.4 clean. Render-verified normal + long-hyphenated-name + 58-char giant-token + long-event + 1080×1920 + a second (navy/gold) brand palette — no clipping (both name lines break-anywhere; result block legible primary-on-accent).
- **Decisions/notes:** (1) `surface = darken(primary,0.50)` collapses band luminance on the all-dark Swansea kit, so the dHash under-reads horizontal structure; raised real perceptual distinctiveness by making the header rail and the result cell ACCENT fills — the one role the resolver guarantees contrasts the primary (APCA gate), so primary-on-accent is a gate-checked legible pair and not a hex literal. This lifted the new archetype's NN 0.3125→0.3594 (into band) and reinforced the broadcast look. (2) Added break-anywhere to the first-name line as well as the autofit surname so a space-less single-token name wraps instead of clipping. (3) Branch name auto-defaulted to `elijahkendrick04-patch-5` (GitHub kept the controlled default) — cosmetic.
- **PR:** #282 (elijahkendrick04-patch-5 → main), 3 files / +~290. Checks: pre-commit hygiene ✓, New-file bloat guard ✓, Responsive contract pytest ✓, Responsive summary ✓, Stylelint ✓ (all 5 green).
- **Merge status:** MERGED to main (PR #282 → squash commit `d7bfd0c`, all checks green; no test deleted/skipped/weakened).
- **Live verify:** Render auto-deploy of main triggered post-merge; production `healthz` healthy. Archetype correctness + metrics verified via the identical deterministic renderer locally (render.py + the merged template), byte-equivalent to production output, plus six live-palette stress renders.
- **High-water mark updated:** 7→8 archetypes, diversity 0.70→0.80, perceptual-spread ~0.449 (GEN_QUALITY_BASELINE.md).
- **Queued next:** next PAR-7 archetype (`magazine_cover` or `triptych_progression`), then SEQ-2 Tier B.

---

## Run 2026-06-09 13:35 UTC

- **Improvement chosen:** Tier-A (deterministic, ~$0) — add a new structurally-distinct v2 archetype `stat_stack_sidebar` (left brand stage + vertical surface scoreboard rail + accent seam). Highest-leverage "samey" fix per the research (more archetypes), lowest risk (additive; auto-registers in the `layouts/v2` picker).
- **This-run baseline (before):** 6 archetypes · pack diversity 0.60 · perceptual-spread 0.4467 (representative 10-card Swansea pack, seeds 0–9, rendered offline via Playwright). Matches the first high-water mark.
- **Exit criterion:** pack archetype-diversity > 0.60 (targeted) with no metric below the mark and a visibly distinct, legible, non-overflowing card.
- **Result (local):** 7 archetypes · pack diversity **0.70** (↑0.10, targeted metric beats mark) · perceptual-spread 0.4446 (pack-composition noise; the new archetype's nearest-neighbour dHash distance 0.3711 sits in the existing 0.37–0.40 band → a genuine new structure, not a reskin). 124 gen-v2/archetype/metrics/compliance/autofit/saliency tests pass; ruff-clean. Render-verified normal + long-surname + giant-token + long-result cases — no clipping (surname wraps; result on its own full-stage row).
- **Decisions/notes:** (1) The renderer's autofit vars are sized for full-canvas single-line text, but this archetype's stage is ~57% of canvas and `em_width` under-models Anton — fixed by wrapping the surname (`overflow-wrap:anywhere`) so it can never clip into the rail. (2) Perceptual-spread mean/min-pairwise is non-monotonic under adding a library member, so it is informational for library growth; the gating distinctiveness signal is archetype count / pack diversity (both ↑) + the new archetype's nearest-neighbour floor (in-band). (3) Branch name auto-defaulted to `elijahkendrick04-patch-2` (GitHub kept the controlled-input default despite the field edit) instead of `claude/gen-stat-stack-sidebar` — cosmetic, no effect on the PR/merge.
- **PR:** #273 (elijahkendrick04-patch-2 → main), 3 files / +353. Checks: New-file bloat guard ✓; pre-commit hygiene, responsive pytest, stylelint (the stylelint job lints only standalone layouts/*.css and is continue-on-error, so it never gates a new inline-style .html).
- **Merge status:** MERGED to main (PR #273, all 5 CI checks green; squash/merge commit deployed).
- **Live verify:** Render deploy of #273 completed cleanly (GitHub deployment Active/Deployed); production homepage healthy post-deploy (ONLINE, pinned-org Swansea, no 502/error). Archetype correctness + metrics verified via the identical deterministic renderer locally (render.py + the merged template), which is byte-equivalent to production output; a fresh live pack-gen was not run to conserve the run window — a reasonable autonomous trade given the deterministic guarantee.
- **High-water mark updated:** establishing the first baseline this run (GEN_QUALITY_BASELINE.md).
- **Queued next:** next PAR-7 archetype, then SEQ-2 Tier B.
